<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Places</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome CSS -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
    <style>
        .travel-image {
            width: 100%;
            height: auto;
        }
        .welcome-container {
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <% if session["user_id"] != nil %>
            <h1>My Places</h1>
            <p>
            <ul class="list-group">
                <% @places.each do |place| %>
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                        <div>
                            <i class="fas fa-city me-2"></i>
                            <%= place['name'] %>
                        </div>
                        <div>
                            <a href="/places/<%= place['id'] %>" class="btn btn-primary btn-sm me-2">View Place</a>
                            <form action="/places/<%= place['id'] %>" method="post" class="d-inline">
                                <input type="hidden" name="_method" value="delete">
                                <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                            </form>
                        </div>
                    </li>
                <% end %>
            </ul>
            <p class="mt-3">
                <a href="/places/new" class="btn btn-success btn-sm">New Place</a>
            </p>
        <% else %>
            <div class="welcome-container">
                <img src="https://images.pexels.com/photos/1285625/pexels-photo-1285625.jpeg?auto=compress&cs=tinysrgb&w=1260&h=750&dpr=2" alt="Travel" class="travel-image mb-4">
                <h1>Welcome!</h1>
                <p>Please sign up or login to get started.</p>
                <p>
                    <a href="/users/new" class="btn btn-success btn-sm">Sign Up</a>
                    <a href="/login" class="btn btn-primary btn-sm">Login</a>
                </p>
            </div>
        <% end %>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
